#include <stdio.h>

/**
  * main - Prints numbers from 0 t0 9
  *
  *Return: Always (Success)
  */
int main(void)

{
	char n;

	for (n = '0'; n <= '9'; c++)
	{
		putchar(n);
	}

	putchar('\n');

	return (0);
}
